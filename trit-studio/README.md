# Trit Studio audit22

C# / Avalonia training lab, TorchSharp CPU/CUDA learning and standalone CPU inference. Full source handoff, not a built binary.
Audit22 adds an opt-in per-batch conversation recipe, crossed phrasing/fact exercises, and a separate62-case development report.
Read docs/TRANSFER_COURSE_RU.md and docs/LEARNING_RESULTS_V22_RU.md. New data/sampling are not a fluency guarantee.

Keep dependencies pinned and emit a small Windows app-only update, not vendor DLLs. Grok begins at GROK_START_HERE.md.
Weights remain V1 with262 UTF8-byte IDs; raw creation takes0 optimizer steps. Existing workspaces/controls are not rewritten.
