# Grok: audit22, learn meaning across formulations, retain real failures

Owner: alinescafs3mp-afk/LLamaTritLLM, existing master. Last verified deployed commit
68727516a4cb06bf99e971991bd95d9dd6632d4c, app tree1c31d80d6dd478091f19d6fa2cc6ff58f864bc5e.
This is cumulative on the exact latest author v21 (388 context records,16 paired checks), not another similarly named prototype.
Keep ALL owner host repairs and original console projects. Never reset/force-push or write upstream.

Read docs/AUDIT_V22.md, TRANSFER_COURSE_RU.md, LEARNING_RESULTS_V22_RU.md and PERFORMANCE_V22.md.
Author did NOT compile C#/run TorchSharp, Avalonia, Windows or CUDA. Python experiments execute another implementation.
No canned decoder answers, hidden pretrained models, benchmark-target injection, waived assertions or language filters.

## Required delivery

Run scripts/deliver.sh or deliver.ps1. Default result is ONLY artifacts/TritStudio-win-x64-update.zip.
Update BOTH own trainer entrypoints and every adjacent data copy, checks and reference fixture. Do NOT add vendor libraries,
change their versions or switch to --full after an update check fails. Preserve installed CUDA/LibTorch/.NET/Avalonia.
Return absolute ZIP path, SHA256, reviewed commit SHA, exact tests executed and separate target-only unrun checks.

## New blocking correctness gates, alongside ALL retained gates

- Build all projects and fix genuine API/type/lifetime errors, not delete tests. Run managed Audit22Checks and real UI Audit22UiChecks.
- Profile explicitly enables TransferPractice. Save never enables it. Parent opt-outs, autosave, next-run per-model restoration,
  repeated Ready, creator independence and stale report identity all retain existing contracts.
- DialogueBatchPlanner chooses a mixture directly. Do NOT call the length-bucket sampler again on that selected mixture.
  Batch1..64, phase transitions, rotating small batches, target counters, original encoded references, cancellation and owned RNG
  must match. Only original final fact targets enter priority fact slots; intermediate acknowledgements remain in general corpus.
- DialogueBatchSelfTest must run on real CPU and actual CUDA: exact planned RNG consumption, batch8/64 update, unchanged ordinary
  evaluation, saved optimizer/RNG and resumed weights. Existing loss/attention/QAT/export/six-reply smoke gates stay mandatory.
- Actual worker direct conversation creation vs raw->close->open->train with TransferPractice must match source data and weights.
- Check conversation is read-only: old10-case report, old16 paired cases and all62 new public development cases. All actual replies
  must be recorded, model step/hash must match. No weight, replay, active pointer or runtime-mode changes on successful check.
- All old data files remain byte-identical. New language/transfer are TRAIN; transfer-challenge is NEVER training. Also verify
  derived assistant-prefix inputs versus controls and public probes. A template count is not a count of independent real conversations.
- Keep keyboard, tooltip, clear-chat, privacy retry, model library/trash, phase-correct creation memory and app-only package tests.

## Learning evidence is separate from build success

Run an isolated native experiment using PERFORMANCE_V22.md. A pilot can be shorter IF reported as such; it cannot be called the
full6000-step course. --enforce-guard is required. Exit3 means rejected candidate, not completed course. CPU is not CUDA.
Report full outputs on both known tasks and new formulations, including bad ones. A falling loss or passed six-reply smoke
is not conversational acceptance. The source includes paired independent6000-step Python trials; read their limitations/results.

Keep the trained user workspaces unchanged until an explicitly requested run. Every new manual course restarts its sampling
phases and LR schedule on current weights, not a newly initialized model. Current corpus/version and source format are in README.
