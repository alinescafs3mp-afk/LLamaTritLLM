# Grok: audit18 cumulative, settings persistence and live progress

Owner fork `alinescafs3mp-afk/LLamaTritLLM`, existing `master`.
Verified deployed audit17: `7bc383e5e6b82fa81f0407e03056369bc8694457`; app tree `5739bd8b99748cfdab83b814f0e2ec54f3f5eff0`.
Preserve all owner repairs and the original console directories. No force/reset/remote upstream writes.

Read docs/AUDIT_V18.md, SETTINGS_AND_PROGRESS_RU.md, PERFORMANCE_V18.md and prior acceptance documents.
The source author DID NOT compile C# or run Avalonia/TorchSharp/Windows/CUDA. Execute tests, do not rebrand structural counts as passes.

## Owner outcome

Run scripts/deliver.sh (or deliver.ps1) from trit-studio. Produce ONLY the default small `artifacts/TritStudio-win-x64-update.zip`.
Both own trainer assemblies and their data must be updated. Do not ship vendor CUDA/LibTorch/Avalonia/.NET again or auto-fallback to --full.
Do not change dependency versions. The existing library manifests and Check-Update contract remain. Update merges files into existing installation.

## Blocking acceptance

All prior core/UI/native/real-worker suites and benchmarks remain required, particularly six-reply LearningSmoke through real gradients,
packed disk and CPU autoregression. No canned answers or softened gates. Keep the independent learned fixture test as a separate decoder check.

New gates:
1. Real textbox input with comma/dot/scientific LR; blur invalid text and save refusal; explicitly refill same-valued invalid fields.
2. Custom LR/steps/batch64/sequence/interval must survive autosave, repeated Ready, model A->B->A, reconnect and app restart.
   Save must NEVER fill the profile. Profile must replace only5 fields and never start training. Creation is separate.
3. Actual worker trains with batch64 and custom LR; committed settings and next launch/reopen agree. UI sidecar is never trainer settings.
4. Exact2sec tooltip dwell on only requested LABELS, including moving straight to a second label. No global tooltip changes.
5. Training accuracy counts only real targets, token-weights unequal batches, evicts after32batches and can decrease. Real control pass
   includes partial final batches, preserves dataset split, caches/invalidation, published metadata, restart/rollback and header model identity.
6. Native LearningProgressSelfTest runs selected/full paths with batch64 and compares counts with teacher oracle. Capture CPU/CUDA evidence.
7. No metrics from QualityProbe's small sample overwrite full control. No per-step forward added just for UI or per-step disk writes.
8. Run GUI test by actual dispatcher/key events. In case of genuine compile/runtime issues, fix causes and rerun, not remove assertions.

Check Windows 100/150/200%DPI with all UI scales. Small-update compatibility must be tested on the owner's installed dependency set.
Before final response commit reviewed changes to owner's existing master (only requested scope). Report absolute update ZIP path,
SHA256, commit SHA, exact completed tests, and explicitly list target-only unrun checks. Never deliver a stale ZIP after a failed build.

## Ongoing dataset

conversation-ru-v18:2826 train,388 control,292 held-out,48 separate basic texts. Byte vocabulary262 and formatV1 unchanged.
Each future audit improves data substantively, with frozen historical holdout+baseline hashes. Do not call the corpus pretrained knowledge
or all2890-style record counts a model-quality score. The user reports language attempts after training, not a confirmed universal chatbot.
